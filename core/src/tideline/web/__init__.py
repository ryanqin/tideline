"""Tideline web playground — FastAPI client over the agent core.

The third client in Tideline's three-layer architecture
(Core ↔ HTTP/JSON ↔ Clients). The CLI client is one entry; this
playground is the other — same core agent, same SQLite store, same
nightly sweep semantics, different surface.
"""
